import { CurrentColorPipe } from './current-color.pipe';

describe('CurrentColorPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrentColorPipe();
    expect(pipe).toBeTruthy();
  });
});
