import { SkipCurrentPipe } from './skip-current.pipe';

describe('SkipCurrentPipe', () => {
  it('create an instance', () => {
    const pipe = new SkipCurrentPipe();
    expect(pipe).toBeTruthy();
  });
});
